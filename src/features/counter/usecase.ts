export class Counter {
  static plus(leftValue: number, rightValue: number) {
    return leftValue + rightValue;
  }

  static minus(leftValue: number, rightValue: number) {
    return leftValue - rightValue;
  }
}
